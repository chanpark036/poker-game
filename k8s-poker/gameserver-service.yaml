apiVersion: v1
kind: Service
metadata:
  labels:
    app: gameserver
  name: gameserver
spec:
  type: NodePort
  ports:
  - name: "gameserver-service"
    port: 5001
    targetPort: 8191
    nodePort: 31001
  selector:
    app: gameserver
